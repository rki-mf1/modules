// TODO nf-core: Once you have added the required tests, please run the following command to build this file:
// nf-core subworkflows test fastq_chopper_nanocomp
nextflow_workflow {

    name "Test Subworkflow FASTQ_CHOPPER_NANOCOMP"
    script "../main.nf"
    workflow "FASTQ_CHOPPER_NANOCOMP"

    tag "subworkflows"
    tag "subworkflows_nfcore"
    tag "subworkflows/fastq_chopper_nanocomp"
    tag "chopper"
    tag "nanocomp"


    // TODO nf-core: Change the test name preferably indicating the test-data and file-format used
    test("sarscov2 - fastq - nanopore") {

        when {
            workflow {
                """
                input[0] = [
                    [
                        [ id:'test1' ],
                        file(params.modules_testdata_base_path + 'genomics/sarscov2/nanopore/fastq/test.fastq.gz', checkIfExists: true)
                    ],
                    [
                        [ id:'test2' ],
                        file(params.modules_testdata_base_path + 'genomics/sarscov2/nanopore/fastq/test_2.fastq.gz', checkIfExists: true)
                    ]
                ]
                """
            }
        }

        then {
            // assert workflow.out.chopped_reads == "foo"
            assertAll(
                { assert workflow.success },
                // { assert snapshot(workflow.out).match()}
                //TODO nf-core: Add all required assertions to verify the test output.
            )
        }
    }
}
